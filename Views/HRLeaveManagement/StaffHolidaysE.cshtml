@model AsteriskBrowserAL.Models.StaffHolidaysData

@{
    ViewBag.Title = "Edit Holidays";
}

@section BreadCrumb{

    <ul class="breadcrumb">
        <li>
            <i class="menu-icon fa fa-list"></i>
            HR
        </li>
        <li>
            <i class=""></i>
            Leave Management
        </li>
        <li class="active">Staff Holidays</li>
    </ul><!-- /.breadcrumb -->
}




@Html.Action("DatePickerScripts", "Base")

<section class="content">

    <br />
    <div class="row">
        <div class="col-xs-12">
            @using (Html.BeginForm("StaffHolidaysE", "HRLeaveManagement", FormMethod.Post, new { @class = "form-horizontal" }))
            {

                <form class="form-horizontal" role="form">
                    @Html.HiddenFor(model => model.StaffHolidaysId)

                    <span style="color:red;font-weight:bold" id="Validation"></span>
                    <div class="page-header">
                        <h1>
                            Staff Holidays
                            <small style="float:right;">
                                @Html.ActionLink("Back to Holidays", "StaffHolidays")
                            </small>
                        </h1>
                    </div>

                    <div class="form-group">
                        <label class="col-sm-3 control-label no-padding-right" for="form-field-1">@Html.Label("Holiday Date")</label>
                        <div class="col-sm-3">
                            <div class="row">
                                <div class="col-xs-12">
                                    <div id="datepicker" class="input-group date">
                                        <input class="form-control" type="text" id="HolidayDate" name="HolidayDate" value="@Model.HolidayDate.Value.ToShortDateString()" d data-date-format="dd/mm/yyyy" placeholder="Select Holiday Date" autocomplete="off" />
                                        <span class="input-group-addon"><i class="ace-icon fa fa-calendar"></i></span>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="col-sm-3" style="display:none;" id="dateHoliday">
                            <label style="color:red;font-weight:bold;"> Required </label>
                        </div>
                    </div>
                    <br />

                    <div class="form-group">
                        <label class="col-sm-3 control-label no-padding-right" for="form-field-1">@Html.Label("Description")</label>

                        <div class="col-sm-3">
                            @Html.TextBoxFor(model => model.Description, new { @class = "form-control textlength", @id = "Desc", placeholder = "Description" })
                        </div>

                        <div class="col-sm-3" style="display:none;" id="descrHoliday">
                            <label style="color:red;font-weight:bold;"> Required </label>
                        </div>
                        <br />
                        <div class="clearfix form-actions">
                            <div class="col-md-offset-3 col-md-9">
                                <button class="btn btn-success btn-sm" onclick="return submitForm();" type="submit">
                                    <i class="ace-icon fa fa-check bigger-50"></i>
                                    Save
                                </button>

                                &nbsp; &nbsp; &nbsp;
                                <button class="btn btn-sm" type="reset">
                                    <i class="ace-icon fa fa-undo bigger-50"></i>
                                    Reset
                                </button>
                            </div>
                        </div>
                    </div>

                </form>

            }
        </div>

    </div>



    <input type="hidden" id="count" name="count" value="0" />
    <input type="hidden" name="OldHolidayDate" id="OldHolidayDate" value="@Model.HolidayDate.Value.ToShortDateString()" />
    <input type="hidden" name="OldDescription" id="OldDescription" value="@Model.Description" />
</section>


<script type="text/javascript">
    function validateHolidays() {

        document.getElementById("count").value = "1";
        var HolidayDate = document.getElementById("HolidayDate").value;
        var Desc = document.getElementById("Desc").value;

        validate = true;

        if (HolidayDate == "") {
            document.getElementById("dateHoliday").style.display = "inline";
            validate = false;
        } else {
            document.getElementById("dateHoliday").style.display = "none";
        }

        if (Desc == "") {
            document.getElementById("descrHoliday").style.display = "inline";
            validate = false;
        } else {
            document.getElementById("descrHoliday").style.display = "none";
        }


        return validate;
    };

    function submitForm() {

        var validatestatus = validateHolidays();
        if (validatestatus == false) {
            return false;
        } else {
            return true;
        }
    };
    $('#HolidayDate').focus(function () {
        $('#dateHoliday').hide();
    });
    $('#Desc').focus(function () {
        $('#descrHoliday').hide();
    });

</script>





