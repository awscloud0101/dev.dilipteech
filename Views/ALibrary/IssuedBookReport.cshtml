
@{
    ViewBag.Title = "IssuedBookReport";
}
<!-- #section:basics/content.breadcrumbs -->

<style type="text/css">
    .ui-autocomplete {
        font-size: 12px;
        cursor: pointer;
        width: 120px;
        height: 120px;
        overflow-y: scroll;
    }
</style>

@section BreadCrumb{
    <ul class="breadcrumb">
        <li>
            <i class="menu-icon fa fa-graduation-cap"></i>
            Academics
        </li>
        <li>
            <i class=""></i>
            Library
        </li>
        <li class="active">Book Issued Report</li>
    </ul>
}
<!-- /section:basics/content.breadcrumbs -->
@Html.Action("DatePickerScripts", "Base", new { IsPaging = false, AllowScroll = true })
<section class="content">
    <div class="row">
        <div class="col-xs-12">
            <div class="table-header">
                <span style="font-family:calibri;"> Book Issued Report</span>
            </div>
            <div class="widget-box">

                <div class="widget-body">
                    <div class="widget-main">
                        <div class="form-group">
                            <div class="row">
                                <div class="col-xs-6" id="ActionDiv">
                                    <label class="col-sm-4 control-label no-padding-right" for="form-field-1">Accession Number</label>
                                    <div class="col-sm-6">
                                        <input type="text" name="BookDetailsId" id="BookDetailsId" spellcheck="false" class="form-control" />
                                    </div>
                                </div>
                                <div class="col-xs-6" id="PublisherDiv">
                                    <label class="col-sm-5 control-label no-padding-right" for="form-field-1">Book Title</label>
                                    <div class="col-sm-6">
                                        <input type="text" name="MasterBookId" id="MasterBookId" spellcheck="false" class="form-control" />
                                    </div>
                                </div>
                            </div>
                        </div>
                        <br />
                        <div class="form-group" id="UserCategoryDiv">
                            <div class="row">
                                <div class="col-xs-6">
                                    <label class="col-sm-4 control-label no-padding-right" for="form-field-1">User Category</label>
                                    <div class="col-sm-6">
                                        @Html.DropDownList("UserCategoryId", null, "--Select--", new { @class = "form-control select2", @id = "UserCategoryId" })
                                    </div>
                                </div>
                                <div class="col-xs-6" id="SearchStudent" style="display:none">
                                    <label class="col-sm-5 control-label no-padding-right" for="form-field-1">Name</label>
                                    <div class="col-sm-6">
                                        <input type="text" name="Student" id="Student" placeholder="Search Student" class="form-control" palceholder="Search" title="Search student with name" autocomplete="off" />
                                    </div>
                                </div>
                                <div class="col-xs-6" id="Searchteacher" style="display:none">
                                    <label class="col-sm-5 control-label no-padding-right" for="form-field-1">Name</label>
                                    <div class="col-sm-6">
                                        <input type="text" name="Teacher" id="Teacher" placeholder="Search Teacher" class="form-control" palceholder="Search" title="Search student with name" autocomplete="off" />
                                    </div>
                                </div>

                            </div>
                        </div>
                        <div class="form-group">
                            <div class="row">
                                <div class="col-xs-6">
                                    <label class="col-sm-4 control-label no-padding-right" for="form-field-1">Date</label>
                                    <div class="col-sm-6">
                                        <div class="row">
                                            <div class="col-xs-12">
                                                <div id="datepicker" class="input-group date">
                                                    <input type="text" id="Date" class="form-control" />
                                                    <span class="input-group-addon "><i class="ace-icon fa fa-calendar"></i></span>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

        </div>
    </div>
    <div class="hr hr-18 dotted hr-double"></div>
</section>
<div id="SearchViewDiv"> </div>
<script>

    function getDepartment() {
        $.ajax({
            url: '/ALibrary/GetDeptName',
            success: function (Data) {
                var DeptDRP = $('#DeptId')
                DeptDRP.children().remove();
                DeptDRP.append($("<option>").val("").text("--Select--"))
                $.each(Data, function (i, record) {
                    DeptDRP.append($("<option>").val(record.DepartmentId).text(record.DepartmentName));
                });
                $('#DeptId').append(DeptDRP);
            },
            async: false,
        })
    }

    function getSection() {
        $.ajax({
            url: '/ALibrary/GetSectionName',
            success: function (Data) {
                var SectionDRP = $('#SectionId')
                SectionDRP.children().remove();
                SectionDRP.append($("<option>").val("").text("--Select--"))
                $.each(Data, function (i, record) {
                    SectionDRP.append($("<option>").val(record.SectionId).text(record.SectionCode));
                });
                $('#SectionId').append(SectionDRP);
            },
            async: false,
        })
    }

    $('#Student').keyup(function (e) {
        debugger
        $("#SearchViewDiv").html("")
        $('#Searchteacher').hide()
        $('#Teacher').val('')
        $('#Date').val('');
        var SectionVal = $('#SectionId').val()
        var userCategory = $('#UserCategoryId').val()
        var Student = $('#Student').val()

        if (Student.length >= 3) {
            $('#imagemodal').show();
            $.ajax({
                url: '/ALibrary/IssueBookReportData',
                type: 'POST',
                data: { name: Student, BookDetailsId: 0, PublicationId: 0, UserCategoryId: userCategory, SectionId: SectionVal, DeptId: 0, AuthorId: 0 },
                success: function (data) {
                    $('#SearchViewDiv').show()
                    $('#SearchViewDiv').html(data);
                    $('#imagemodal').hide();
                },
                error: function () {
                    $('#imagemodal').hide();
                },
            }).done(function () {
                $('#imagemodal').hide();
            })
        }
    });
    $('#Teacher').keyup(function (e) {
        debugger
        $("#SearchViewDiv").html("")
        var DeptId = $('#DeptId').val()
        $('#Date').val('');
        var userCategory = $('#UserCategoryId').val()
        var Teacher = $('#Teacher').val()

        if (Teacher.length >= 3) {
            $('#imagemodal').show();
            debugger
            $.ajax({
                url: '/ALibrary/IssueBookReportData',
                type: 'POST',
                data: { name: Teacher, BookDetailsId: 0, PublicationId: 0, UserCategoryId: userCategory, SectionId: 0, DeptId: DeptId, AuthorId: 0 },
                success: function (data) {
                    debugger
                    $('#SearchViewDiv').show()
                    $('#SearchViewDiv').html(data);
                    $('#imagemodal').hide();
                },
                error: function () {
                    $('#imagemodal').hide();
                },
            }).done(function () {
                $('#imagemodal').hide();
            })
        }
    });
    $('#Date').change(function (e) {
        debugger
        $('#Search').hide()
        $('#Student').val('')
        $('#Searchteacher').hide()
        $('#Teacher').val('')
        $('#SearchViewDiv').hide()
        $('#StaffUser').hide();
        $('#StudentUser').hide();
        $('#DeptId').val('');
        $('#SectionId').val('');
        $('#MasterBookId').val('').trigger('change.select2');
        $('#UserCategoryId').val('').trigger('change.select2');
        $('#AuthorId').val('').trigger('change.select2');
        $('#BookDetailsId').val('').trigger('change.select2');
        $('#PublicationId').val('').trigger('change.select2');
        $("#SearchViewDiv").html("")
        var Date = $('#Date').val()
        $('#imagemodal').show();
        $.ajax({
            url: '/ALibrary/GetIssueBookDateWise',
            type: 'POST',
            data: { date: Date },
            success: function (data) {
                $('#SearchViewDiv').show()
                $('#SearchViewDiv').html(data);
                $('#imagemodal').hide();
            },
            error: function () {
                $('#imagemodal').hide();
            },
        }).done(function () {
            $('#imagemodal').hide();
        })
    });



    $('#UserCategoryId').change(function () {
        debugger
        $('#Search').hide()
        $('#Date').val('');
        $('#Student').val('')
        $('#Searchteacher').hide()
        $('#Teacher').val('')
        $('#SearchViewDiv').hide()
        $('#MasterBookId').val('').trigger('change.select2');
        $('#PublicationId').val('').trigger('change.select2');
        $('#AuthorId').val('').trigger('change.select2');
        $('#BookDetailsId').val('').trigger('change.select2');
        $('#DeptId').val('');
        $('#SectionId').val('');
        var UserCategoryVal = $('#UserCategoryId').val();
        if (UserCategoryVal == 1) {
            $('#StaffUser').show();
            $('#StudentUser').hide();
            $('#Searchteacher').show();
            $('#SearchStudent').hide();
        }
        else if (UserCategoryVal == 2) {
            $('#StudentUser').show();
            $('#StaffUser').hide();
            $('#Searchteacher').hide();
            $('#SearchStudent').show();
        }
        else if (UserCategoryVal == 0) {
            $('#StaffUser').val('');
            $('#StudentUser').val('');
            $('#StaffUser').hide();
            $('#StudentUser').hide();
            var userCategory = $('#UserCategoryId').val();
            $('#imagemodal').show();
            $.ajax({
                url: '/ALibrary/IssueBookReportData',
                type: 'POST',
                data: { BookDetailsId: 0, PublicationId: 0, UserCategoryId: userCategory, SectionId: 0, DeptId: 0, AuthorId: 0 },
                success: function (data) {
                    $('#SearchViewDiv').show()
                    $('#SearchViewDiv').html(data);
                    $('#imagemodal').hide();
                },
                error: function () {
                    $('#imagemodal').hide();
                },
            }).done(function () {
                $('#imagemodal').hide();
            })
        }
    })

    $('#SectionId').change(function () {
        $('#Search').show()
        $('#Searchteacher').hide()
        $('#Teacher').val('')
        $('#Date').val('');
        $('#SearchViewDiv').hide()
        $('#MasterBookId').val('').trigger('change.select2');
        $('#PublicationId').val('').trigger('change.select2');
        $('#AuthorId').val('').trigger('change.select2');
        $('#BookDetailsId').val('').trigger('change.select2');
        $('#DeptId').val('');
        var SectionVal = $('#SectionId').val();
        var userCategory = $('#UserCategoryId').val()
        if (SectionVal == null || SectionVal == "" || SectionVal == undefined) {
        }
        else {
            $('#imagemodal').show();
            $.ajax({
                url: '/ALibrary/IssueBookReportData',
                type: 'POST',
                data: { BookDetailsId: 0, PublicationId: 0, UserCategoryId: userCategory, SectionId: SectionVal, DeptId: 0, AuthorId: 0 },
                success: function (data) {
                    $('#SearchViewDiv').show()
                    $('#SearchViewDiv').html(data);
                    $('#imagemodal').hide();
                },
                error: function () {
                    $('#imagemodal').hide();
                },
            }).done(function () {
                $('#imagemodal').hide();
            })
        }
    })

    $('#DeptId').change(function () {
        $('#Search').hide()
        $('#Student').val('')
        $('#Searchteacher').show()
        $('#Date').val('');
        $('#Teacher').val('')
        $('#SearchViewDiv').hide()
        $('#MasterBookId').val('').trigger('change.select2');
        $('#PublicationId').val('').trigger('change.select2');
        $('#AuthorId').val('').trigger('change.select2');
        $('#BookDetailsId').val('').trigger('change.select2');
        $('#SectionId').val('');
        var DeptVal = $('#DeptId').val();
        var userCategory = $('#UserCategoryId').val()
        if (DeptVal == null || DeptVal == "" || DeptVal == undefined) {
        }
        else {
            $('#imagemodal').show();
            $.ajax({
                url: '/ALibrary/IssueBookReportData',
                type: 'POST',
                data: { BookDetailsId: 0, PublicationId: 0, UserCategoryId: userCategory, SectionId: 0, DeptId: DeptVal, AuthorId: 0 },
                success: function (data) {
                    $('')
                    $('#SearchViewDiv').show()
                    $('#SearchViewDiv').html(data);
                    $('#imagemodal').hide();
                },
                error: function () {
                    $('#imagemodal').hide();
                },
            }).done(function () {
                $('#imagemodal').hide();
            })
        }
    })

    //$('#BookDetailsId').change(function () {
    //    $('#Search').hide()
    //    $('#Date').val('');
    //    $('#Student').val('')
    //    $('#Searchteacher').hide()
    //    $('#Teacher').val('')
    //    $('#SearchViewDiv').hide()
    //    $('#StudentUser').hide()
    //    $('#StaffUser').hide()
    //    $('#MasterBookId').val('').trigger('change.select2');
    //    $('#UserCategoryId').val('').trigger('change.select2');
    //    $('#PublicationId').val('').trigger('change.select2');
    //    $('#AuthorId').val('').trigger('change.select2');

    //    $('#DeptId').val('');
    //    $('#SectionId').val('');
    //    $('#UserCategoryId').val('');

    //    var BookDetailsId = $('#BookDetailsId').val();
    //    if (BookDetailsId == null || BookDetailsId == "" || BookDetailsId == undefined) {
    //    }
    //    else {
    //        $('#imagemodal').show();
    //        $.ajax({
    //            url: '/ALibrary/IssueBookReportData',
    //            type: 'POST',
    //            data: { BookDetailsId: BookDetailsId, PublicationId: 0, UserCategoryId: 0, SectionId: 0, DeptId: 0, AuthorId: 0 },
    //            success: function (data) {
    //                $('#SearchViewDiv').show()
    //                $('#SearchViewDiv').html(data);
    //                $('#imagemodal').hide();
    //            },
    //            error: function () {
    //                $('#imagemodal').hide();
    //            },
    //        }).done(function () {
    //            $('#imagemodal').hide();
    //        })
    //    }
    //})



    //$('#MasterBookId').change(function () {
    //    $('#Search').hide()
    //    $('#Date').val('');
    //    $('#Student').val('')
    //    $('#Searchteacher').hide()
    //    $('#Teacher').val('')
    //    $('#SearchViewDiv').hide()
    //    $('#StudentUser').hide()
    //    $('#StaffUser').hide()
    //    $('#PublicationId').val('').trigger('change.select2');
    //    $('#BookDetailsId').val('').trigger('change.select2');
    //    $('#UserCategoryId').val('').trigger('change.select2');
    //    $('#DeptId').val('');
    //    $('#SectionId').val('');
    //    $('#UserCategoryId').val('');

    //    var MasterBookId = $('#MasterBookId').val();

    //    if (MasterBookId == null || MasterBookId == "" || MasterBookId == undefined) {
    //    }
    //    else {
    //        $('#imagemodal').show();
    //        $.ajax({
    //            url: '/ALibrary/IssueBookReportData',
    //            type: 'POST',
    //            data: { BookDetailsId: 0, PublicationId: 0, UserCategoryId: 0, SectionId: 0, DeptId: 0, AuthorId: 0, MasterBookId: MasterBookId },
    //            success: function (data) {
    //                $('#SearchViewDiv').show()
    //                $('#SearchViewDiv').html(data);
    //                $('#imagemodal').hide();
    //            },
    //            error: function () {
    //                $('#imagemodal').hide();
    //            },
    //        }).done(function () {
    //            $('#imagemodal').hide();
    //        })
    //    }
    //})

    $('#AuthorId').change(function () {
        $('#Search').hide()
        $('#Date').val('');
        $('#Student').val('')
        $('#Searchteacher').hide()
        $('#Teacher').val('')
        $('#SearchViewDiv').hide()
        $('#StudentUser').hide()
        $('#StaffUser').hide()
        $('#MasterBookId').val('').trigger('change.select2');
        $('#PublicationId').val('').trigger('change.select2');
        $('#BookDetailsId').val('').trigger('change.select2');
        $('#DeptId').val('');
        $('#SectionId').val('');
        $('#UserCategoryId').val('');

        var AuthorId = $('#AuthorId').val();

        if (AuthorId == null || AuthorId == "" || AuthorId == undefined) {
        }
        else {
            $('#imagemodal').show();
            $.ajax({
                url: '/ALibrary/IssueBookReportData',
                type: 'POST',
                data: { BookDetailsId: 0, PublicationId: 0, UserCategoryId: 0, SectionId: 0, DeptId: 0, AuthorId: AuthorId },
                success: function (data) {
                    $('#SearchViewDiv').show()
                    $('#SearchViewDiv').html(data);
                    $('#imagemodal').hide();
                },
                error: function () {
                    $('#imagemodal').hide();
                },
            }).done(function () {
                $('#imagemodal').hide();
            })
        }
    })

    $('#PublicationId').change(function () {
        debugger
        $('#Search').hide()
        $('#Student').val('')
        $('#Date').val('');
        $('#Searchteacher').hide()
        $('#Teacher').val('')
        $('#StudentUser').hide()
        $('#StaffUser').hide()
        $('#SearchViewDiv').hide()
        $('#MasterBookId').val('').trigger('change.select2');
        $('#AuthorId').val('').trigger('change.select2');
        $('#BookDetailsId').val('').trigger('change.select2');
        $('#DeptId').val('');
        $('#SectionId').val('');
        $('#UserCategoryId').val('');

        var PublicationId = $('#PublicationId').val();
        if (PublicationId == null || PublicationId == "" || PublicationId == undefined) {

        }
        else {
            $('#imagemodal').show();
            $.ajax({
                url: '/ALibrary/IssueBookReportData',
                type: 'POST',
                data: { BookDetailsId: 0, PublicationId: PublicationId, UserCategoryId: 0, SectionId: 0, DeptId: 0, AuthorId: 0 },
                success: function (data) {
                    $('#SearchViewDiv').show()
                    $('#SearchViewDiv').html(data);
                    $('#imagemodal').hide();
                },
                error: function () {
                    $('#imagemodal').hide();
                },
            }).done(function () {
                $('#imagemodal').hide();
            })
        }
    })

</script>
<script type="text/javascript">
    $("#BookDetailsId").autocomplete(
        {
            source: function (request, response) {



                if (request.term.length < 0) {
                    return;
                }
                $('#imagemodal').show();

                $.ajax({
                    url: "/ALibrary/GetIssuedBookCode",
                    type: "POST",
                    dataType: "json",
                    data: {
                        strVal: request.term
                    },
                    success: function (json) {
                        debugger;
                        response($.map(json, function (jsondata, id) {
                            return {
                                label: jsondata.BookCode,
                                value: jsondata.BookCode,
                                id: jsondata.BookCode,
                            };
                        }));
                        $('#imagemodal').hide();
                    },
                });
            },
            minLength: 1,
            select: function SelectEvent(event, ui) {

                bookid = ui.item.value;
                GetBookDetails(bookid, 0);
                // (ui.item.value, "BookCode");
            }
        });

    $("#MasterBookId").autocomplete(
        {
            source: function (request, response) {



                if (request.term.length < 0) {
                    return;
                }
                $('#imagemodal').show();

                $.ajax({
                    url: "/ALibrary/GetAllBooks",
                    type: "POST",
                    dataType: "json",
                    data: {
                        strVal: request.term
                    },
                    success: function (json) {
                        debugger;
                        response($.map(json, function (jsondata, id) {
                            return {
                                label: jsondata.BookName,
                                value: jsondata.BookName,
                                id: jsondata.MasterBookId,
                            };
                        }));
                        $('#imagemodal').hide();
                    },
                });
            },
            minLength: 1,
            select: function SelectEvent(event, ui) {
                debugger
                MasterBookId = ui.item.id;
                GetBookDetails(0, MasterBookId);
                // (ui.item.value, "BookCode");
            }
        });
</script>

<script type="text/javascript">
    function GetBookDetails(bookCodeId, MasterBookId) {
        $('#Search').hide()
        $('#Date').val('');
        $('#Student').val('')
        $('#Searchteacher').hide()
        $('#Teacher').val('')
        $('#SearchViewDiv').hide()
        $('#StudentUser').hide()
        $('#StaffUser').hide()
        $('#DeptId').val('');
        $('#SectionId').val('');
        $('#UserCategoryId').val('');
        $('#imagemodal').show();
        $.ajax({
            url: '/ALibrary/IssueBookReportData',
            type: 'POST',
            data: { BookDetailsId: bookCodeId, PublicationId: 0, UserCategoryId: 0, SectionId: 0, DeptId: 0, AuthorId: 0, MasterBookId: MasterBookId },
            success: function (data) {
                $('#SearchViewDiv').show()
                $('#SearchViewDiv').html(data);
                $('#imagemodal').hide();
            },
            error: function () {
                $('#imagemodal').hide();
            },
        }).done(function () {
            $('#imagemodal').hide();
        })
    }
</script>